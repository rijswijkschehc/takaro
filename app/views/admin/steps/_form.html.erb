<%= simple_form_for([:admin, @step], data: { turbo: false }) do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :rich_text_area %>
  <%= f.input :icon %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
