<%= simple_form_for([:admin, @period]) do |f| %>
  <%= f.input :name %>
  <%= f.input :description, as: :rich_text_area %>
  <%= f.input :starts_on %>
  <%= f.input :ends_on %>
  <%= f.submit %>
<% end %>
