<%= simple_form_for([:admin, @location], data: { turbo: false }) do |f| %>
  <%= f.input :name %>
  <%= f.input :owner %>
  <%= f.association :parent %>
  <%= f.input :lock_id, collection: @available_locks, label_method: :number %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
