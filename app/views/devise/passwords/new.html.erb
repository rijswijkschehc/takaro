<div class="row justify-content-center">
  <div class="col-sm-10 col-md-8 col-lg-4 mt-5">
    <div class="card">
      <div class="card-header"><%= _('Forgot your password?') %></div>
      <div class="card-body">
        <%= simple_form_for(resource,
                            as: resource_name,
                            url: password_path(resource_name),
                            wrapper: :floating_labels_form,
                            defaults: { placeholder: 'true' },
                            html: { method: :post }) do |f| %>
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: 'email' } %>
          <%= f.submit _('Send me reset password instructions'), class: 'btn btn-primary w-100' %>
        <% end %>
      </div>
    </div>
    <div class="card-footer text-center">
      <%= render 'devise/shared/links' %>
    </div>
  </div>
</div>
