<div class="row">
  <div class="col">
    <h1>
      <%= icon(id: 'book') %>
      <%= @training.title %>
      <% if policy(@training).edit? %>
        <%= link_to icon(id: 'edit'), edit_training_path(@training) %>
      <% end %>
    </h1>
  </div>
</div>
<div class="row border-bottom">
  <div class="col">
    <%= @training.step.badge %>
    <%= safe_join(@training.principles.map(&:badge)) %>
    <p class="lead"><%= @training.objective %></p>
  </div>
</div>
